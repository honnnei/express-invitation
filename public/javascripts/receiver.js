document.addEventListener("DOMContentLoaded", function() {
    console.log('it works');

    // async function formValues() {
    //     let greeting = await document.getElementById("form#greeting").value;
    //     console.log(greeting);
    // } 

    // formValues();
});